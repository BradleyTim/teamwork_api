const expect = require('chai').expect;
const user = require('../../routes/api/users');

describe('/signin', function() {
  it('should sign in user and return id and token', function(done) {
  	expect('test').to.be.a('string');
  	done();
  });
});